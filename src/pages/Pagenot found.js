import React from 'react'
import Layout from '../components/Layout.js/Layout'

const Pagenot found() {
    return ( <
        Layout >
        <
        h1 > page not found < /h1> <
        /Layout>
    )
}

export default Pagenot found;